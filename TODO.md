# CLI
- [] Probably the best approach would be to integrate Cargo Watch directly into the CLI, instead of using it as an extension.
- [] need to fix modules export imports, public attribute with settings, default as private if not configured

